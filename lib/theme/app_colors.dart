import 'package:flutter/material.dart';

class AppColors {
  // 🔹 PRIMARY BRAND
  static const Color primary = Color(0xFF4A90E2); // Core Blue
  static const Color primaryDark = Color(0xFF0063B1);
  static const Color primaryLight = Color(0xFFE9F3FF); // Light Blue bg

  // 🔹 SECONDARY / ACCENT
  static const Color accent = Color(0xFF50E3C2); // Teal/Cyan for success/safe
  static const Color accentPurple = Color(0xFF9013FE); // Premium/AI features

  // 🔹 SEMANTIC STATUS
  static const Color success = Color(0xFF2ECC71);
  static const Color warning = Color(0xFFF5A623);
  static const Color error = Color(0xFFE74C3C);
  static const Color info = Color(0xFF3498DB);

  // 🔹 NEUTRALS (Text & Backgrounds)
  static const Color textPrimary = Color(0xFF1C1C1E); // Nearly black
  static const Color textSecondary = Color(0xFF8E8E93); // Grey text
  static const Color textPlaceholder = Color(0xFFC7C7CC);

  static const Color backgroundLight = Color(0xFFF2F6FA); // Slightly grey-blue standard bg
  static const Color surfaceWhite = Colors.white;
  static const Color surfaceGlass = Color(0xCCFFFFFF); // 80% opacity white for glassmorphism

  static const Color border = Color(0xFFE5E5EA);
}
